import { rem } from "polished";
import styled from "styled-components";

export const DashboardContent = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 50vh;
  row-gap: ${rem(24)};
  padding: ${rem(24)};
  overflow: auto;
`;

export const OpportunitiesDocs = styled.div`
  /* Layout */
  /* Element Styles */
  /* Text styles */
`;
