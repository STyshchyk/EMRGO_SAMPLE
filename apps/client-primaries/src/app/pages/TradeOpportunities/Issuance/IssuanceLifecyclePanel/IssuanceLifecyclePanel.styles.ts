import { Hyperlink } from "@emrgo-frontend/shared-ui";
import styled from "styled-components";

export const AddToCalendarLink = styled(Hyperlink)`
  cursor: pointer;
  background-color: transparent;
  border: none;
`;
