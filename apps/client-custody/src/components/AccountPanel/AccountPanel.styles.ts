import { Panel } from "@emrgo-frontend/shared-ui";
import { rem } from "polished";
import styled from "styled-components";

export const AccountPanel = styled(Panel)`
  row-gap: ${rem(16)};
  padding: ${rem(24)};
`;
