import { render } from "@testing-library/react";

import { AccountPanelHeaderTitle } from "./AccountPanelHeaderTitle";

test("AccountPanelHeaderTitle", () => {
  render(<AccountPanelHeaderTitle />);
});
