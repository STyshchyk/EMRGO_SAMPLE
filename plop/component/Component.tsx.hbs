import { FC } from "react";

import * as Styles from './{{pascalCase name}}.styles';
import { I{{pascalCase name}}Props} from './{{pascalCase name}}.types';

export const {{pascalCase name}}: FC< I{{pascalCase name}}Props> = ({}) => {
  return (
    <Styles.{{pascalCase name}}>
      {{pascalCase name}}
    </Styles.{{pascalCase name}}>
  );
};
