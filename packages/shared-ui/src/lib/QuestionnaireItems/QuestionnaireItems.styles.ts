import { rem } from "polished";
import styled from "styled-components";

export const QuestionnaireItems = styled.ul`
  display: flex;
  flex-direction: column;
  row-gap: ${rem(24)};
  padding: 0;
  margin: 0;
  list-style: none;
`;
